name: Build

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  server-build:
    name: Server Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache: false

      - name: Download dependencies
        working-directory: ./server
        run: go mod download

      - name: Verify build
        working-directory: ./server
        run: |
          go build -v ./...
          echo "✓ Server builds successfully"

  member-build:
    name: Member Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v3/cmd/wails3@v3.0.0-alpha.60

      - name: Generate Wails bindings
        working-directory: ./member
        run: wails3 generate bindings -clean=true -ts

      - name: Install frontend dependencies
        working-directory: ./member/frontend
        run: npm install

      - name: Build frontend assets
        working-directory: ./member/frontend
        run: npm run build

      - name: Download Go dependencies
        working-directory: ./member
        run: go mod download

      - name: Verify build
        working-directory: ./member
        run: |
          go build -v ./...
          echo "✓ member builds successfully"

  manager-build:
    name: Manager Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'
          cache-dependency-path: 'manager/pnpm-lock.yaml'

      - name: Install dependencies
        working-directory: ./manager
        run: pnpm install --frozen-lockfile

      - name: Build manager UI
        working-directory: ./manager
        run: pnpm build
        env:
          CI: true

      - name: Verify build output
        run: |
          if [ ! -d "manager/dist" ]; then
            echo "Build output directory not found"
            exit 1
          fi
          echo "✓ Manager UI builds successfully"
